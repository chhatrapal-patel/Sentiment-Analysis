# Sentiment-Analysis
A machine learning project that performs sentiment analysis on hotel reviews using a Transformer-based model (DistilBERT). The system classifies reviews into Positive, Neutral, and Negative categories with data preprocessing, model training, and performance evaluation using real-world TripAdvisor data.
